{
  "name": "token-perf",
  "version": "0.0.0",
  "type": "module",
  "bin": { "token-perf": "dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.3.0",
    "@types/node": "^26.4.0",
    "svelte": "^5.56.10",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
